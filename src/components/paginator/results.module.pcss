.results {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 5px;
  font-weight: lighter;
  font-size: 0.9em;
}

.select-per-page {
  padding: 3px;
  text-align: center;
  option:nth-child(2n) {
    background-color: #0002;
  }
}
