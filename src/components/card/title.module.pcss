.header {
  display: flex;
  flex-direction: column;
  gap: 5px;
  padding: 5px 10px;
  background: #000a;
  backdrop-filter: blur(5px);
}

.title {
  color: #fff;
  font-size: 1em;
  font-weight: normal;
  //overflow: hidden;
  //white-space: nowrap;
  //text-overflow: ellipsis;
}

.studio {
  --color: #0ff;
  font-family: var(--ff-code);
  font-size: 0.8em;
  color: var(--color);
  fill: var(--color);
}
